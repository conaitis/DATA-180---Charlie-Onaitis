library(dplyr)
library(dplyr)
records <- read.csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2021/2021-05-25/records.csv')
head(records)
install.packages("dplyr")
library(dplyr)
records <- read.csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2021/2021-05-25/records.csv')
head(records)
install.packages("dplyr")
summary(records)
records <- records %>%
summary(records)
records %>%
summary(records)
summary(records)
arrange(record_date)
records <- records %>%
records <- records %>%
arrange(record_date) %>%
mutate(diff_days)
records <- records %>%
arrange(record_date = c(NA, diff(record_date))) %>%
mutate(diff_days)
records <- records %>%
arrange(record_duration = c(NA, diff(record_duration))) %>%
mutate(diff_days)
arrange(record_duration = c(NA, diff(record_duration)))
arrange(record_duration = c(NA, diff(record_duration)))
arrange(data=records, record_duration = c(NA, diff(record_duration)))
